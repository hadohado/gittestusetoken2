//
//  ContentView.swift
//  gittestusetoken
//
//  Created by ha tuong do on 1/25/24.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("git test after delete xcode Git acct and re-create new git acct with token, world!")
        }
        .padding()
    }
}

//#Preview {
//    ContentView()
//}
